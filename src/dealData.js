export default class DealData {
  static dealOperation(e) {
    console.log(e.target.innerText);
  }

  static dealNumber(e) {
    console.log(e.target.innerText);
  }

  static dealEqual(e) {
    console.log(e.target.innerText);
  }
}